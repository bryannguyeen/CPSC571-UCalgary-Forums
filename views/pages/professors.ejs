
<!DOCTYPE html>
<html lang="en">

  <% include ../partials/head %>
  <body>

    <% include ../partials/navbar %>


    <div class="container">

      <div class="row row-offcanvas row-offcanvas-right">

        <div class="col-12 col-md-9">
          <p class="float-right hidden-md-up">
            <button type="button" class="btn btn-primary btn-sm" data-toggle="offcanvas">Toggle nav</button>
          </p>
          <h1>Professors</h1>
          <small>Don't see your professor? <a href='/newprofessor'>Add a new professor</a></small>
          <div class="jumbotron">
            <% for (var i=0; i < locals.professors.length; i++) { %>
              <a href="#"><h4><%= locals.professors[i].firstname %> <%= locals.professors[i].lastname %></h4></a>
            <% } %>
          </div>
        </div>
        <%- include('../partials/sidebar', {active: 'professors'}); %>

      </div>

      <hr>
    </div>
  </body>
</html>
